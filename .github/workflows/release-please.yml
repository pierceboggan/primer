name: Release Please

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
      ext_release_created: ${{ steps.release.outputs['vscode-extension--release_created'] }}
      ext_tag_name: ${{ steps.release.outputs['vscode-extension--tag_name'] }}
    steps:
      - name: Release
        id: release
        uses: googleapis/release-please-action@v4
        with:
          config-file: release-please-config.json
          manifest-file: release-please-manifest.json

  build-extension-cross-platform:
    name: Build Extension (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: release-please
    if: ${{ needs.release-please.outputs.ext_release_created == 'true' }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            vscode-extension/package-lock.json
      - run: npm ci --ignore-scripts
      - run: npm ci
        working-directory: vscode-extension
      - name: Typecheck extension
        run: npx tsc --noEmit
        working-directory: vscode-extension
      - name: Build extension
        run: node esbuild.mjs --production
        working-directory: vscode-extension

  package-extension:
    name: Package Extension
    runs-on: ubuntu-latest
    needs: [release-please, build-extension-cross-platform]
    if: ${{ needs.release-please.outputs.ext_release_created == 'true' && needs.build-extension-cross-platform.result == 'success' }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            vscode-extension/package-lock.json
      - run: npm ci
      - run: npm ci
        working-directory: vscode-extension
      - name: Build extension
        run: node esbuild.mjs --production
        working-directory: vscode-extension
      - name: Package VSIX
        run: npx @vscode/vsce package --no-dependencies
        working-directory: vscode-extension
      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: extension-vsix
          path: vscode-extension/*.vsix
      - name: Upload VSIX to release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_NAME: ${{ needs.release-please.outputs.ext_tag_name }}
        run: gh release upload --clobber "$TAG_NAME" vscode-extension/*.vsix

  publish-extension:
    name: Publish Extension
    runs-on: ubuntu-latest
    needs: [release-please, package-extension]
    if: ${{ needs.release-please.outputs.ext_release_created == 'true' && needs.package-extension.result == 'success' }}
    permissions:
      contents: read
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Download VSIX artifact
        uses: actions/download-artifact@v4
        with:
          name: extension-vsix
          path: vsix
      - name: Publish to VS Code Marketplace
        run: npx @vscode/vsce@latest publish --packagePath vsix/*.vsix
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}

  publish-cli:
    name: Publish CLI to npm
    runs-on: ubuntu-latest
    needs: release-please
    if: ${{ needs.release-please.outputs.release_created == 'true' }}
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run test
      - run: npm run build
      - run: npm publish --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
